name: Website

on:
    workflow_dispatch:

permissions: {}

jobs:
    website-deploy:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v7

            - run: |
                  echo Please run it from the "site" branch.
                  exit 1
